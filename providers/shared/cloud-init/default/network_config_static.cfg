network:
  version: 2
  ethernets:
    enp1s0:
      addresses:
        - {{ vm_name.value.ip }}/24
      routes:
        - to: default
          via: {{ vm_gateway }}
      nameservers:
        addresses: [${dns_servers}]
        search: [${domain}]